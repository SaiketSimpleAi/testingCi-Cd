{
  "name": "notes-api",
  "version": "1.0.0",
  "description": "Simple Node.js + PostgreSQL API with a full Docker-based CI/CD pipeline",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "test": "jest --runInBand --forceExit",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "eslint": "^9.7.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}

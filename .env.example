# Copy to .env for local runs:  cp .env.example .env
PORT=3000

# Local Postgres (matches docker-compose.yml).
DATABASE_URL=postgres://notes:notes@localhost:5432/notes

# Set to "true" against managed Postgres (Railway, RDS, etc.) that requires TLS.
PGSSL=false
